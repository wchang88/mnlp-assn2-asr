# mnlp-assn2-asr
Winnie Chang (winniech) &amp; Yash Mathur (ymathur)

Directory Structure
```
guarani
  -local
    =data.sh
    =data_prep files
  -baseline
    -conf
      =configuration files
    =run.sh files
  -pretrained
    -conf
      =configuration files
    =run.sh files
  -multilingual
    -conf
      =configuration files
    =run.sh files
```
